annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output

# Build directories
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
build.classes.dir=${build.dir}/classes
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
build.classes.excludes=**/*.java,**/*.form

# Dist directories
dist.dir=dist
dist.jar=${dist.dir}/WorkoutLogger.jar
dist.javadoc.dir=${dist.dir}/javadoc

# Source directories
src.dir=src
test.src.dir=test

# Classpath (includes MySQL connector)
file.reference.mysql-connector-j.jar=lib/mysql-connector-j.jar
javac.classpath=${file.reference.mysql-connector-j.jar}

# Encoding and compiler options
source.encoding=UTF-8
javac.source=1.8
javac.target=1.8

# Main class
main.class=com.ayush.workoutlogger.Main
